# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.

Note: This project was downgraded from SDK 56 to SDK 54 because the iOS App Store
version of Expo Go (54.0.2) only supports SDK 54. Once Expo Go for SDK 56+ ships
on the App Store, the project can be upgraded back with `npx expo install expo@latest --fix`.
